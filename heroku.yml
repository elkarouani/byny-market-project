setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

  config:
    env_file: front/.env, back/.env

build:
  docker:
    web_server: Dockerfile.back.prod
    web_client: Dockerfile.front.prod

  config:
    NODE_ENV: production