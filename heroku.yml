setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

  config:
    env_file: .env

build:
  docker:
    web: Dockerfile.router
    web_server: Dockerfile.back.prod
    web_client: Dockerfile.front.prod

  config:
    NODE_ENV: production