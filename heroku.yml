setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

  config:
    env_file: front/.env, back/.env

build:
  docker:
    web: Dockerfile.front.prod
    backend: Dockerfile.back.prod

  config:
    NODE_ENV: production